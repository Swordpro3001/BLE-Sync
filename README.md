# BLE-Sync
Angular-based Progressive Web App (PWA) that uses the Capacitor Bluetooth Low Energy (BLE) API to sync data directly between nearby devices on Android and iOS. The project focuses on secure, serverless peer-to-peer communication and reliable offline data transfer.
